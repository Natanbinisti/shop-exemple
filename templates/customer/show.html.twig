{% block title %}


{% endblock %}
{% extends 'base.html.twig' %}
{% block body %}

    <h3>Product : {{ product.name }}</h3>
    <div class="border border-dark">
        <p>Price : </p>
        <p>Date added : </p>

        <a href="{{ path('app_customer_index') }}" class="btn btn-secondary">return</a>
        <a href="" class="btn btn-success">Add to cart</a>
    </div>

{% endblock %}
